import FlashcardsClient from "./components/flashcards-client";

export const metadata = {
  title: "Nursing Flashcards | STEMRN",
  description: "Review high-yield concepts, master difficult cards, and retain more with interactive flashcards.",
};

export default function FlashCardsPage() {
  return <FlashcardsClient />;
}